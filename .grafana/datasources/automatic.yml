apiVersion: 1

deleteDatasource:
  - name: Prometheus
    orgId: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    basicAuth: false
    jsonData:
      httpMethod: POST